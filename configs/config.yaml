# Main Hydra configuration entrypoint

defaults:
  - experiment: base
  - data: dukascopy
  - assets: EURUSD
  - session: default_fx
  - labeling: triple_barrier
  - features: base_features
  - models: rf_gpu  # GPU by default for training
  - validation: tscv
  - risk: basic
  - backtest: base_bt
  - reporting: report
  - mlflow: local
  - runtime: hardware
  - _self_

# Override from command line with: +key=value

