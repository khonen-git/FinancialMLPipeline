name: financial-ml
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.12
  - numpy>=1.24.0
  - pandas>=2.0.0
  - polars>=0.19.0
  - pyarrow>=13.0.0
  - scikit-learn>=1.3.0
  - pip
  - pip:
      - hmmlearn>=0.3.0
      - backtrader>=1.9.76
      - hydra-core>=1.3.0
      - mlflow>=2.8.0
      - jinja2>=3.1.0
      - pytest>=7.4.0
      - pytest-cov>=4.1.0
      - black>=23.0.0
      - ruff>=0.1.0
      - mypy>=1.5.0

# Optional GPU support (uncomment if needed)
# For RAPIDS cuML with RTX 4070 (CUDA 12.x), use:
# 
# Option 1: Install via micromamba (recommended)
# micromamba install -c rapidsai -c conda-forge -c nvidia \
#     cuml=24.02 cudatoolkit=12.4 python=3.12
#
# Option 2: Add to dependencies (uncomment below)
# - cudatoolkit=12.4
# - cuml=24.02
#
# Note: RTX 4070 supports CUDA 12.x, so use cudatoolkit=12.4
# Verify CUDA version: nvidia-smi

