name: financial-ml
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.12
  - numpy>=1.24.0
  - pandas>=2.0.0
  - polars>=0.19.0
  - pyarrow>=13.0.0
  - scikit-learn>=1.3.0
  - pip
  - pip:
      - hmmlearn>=0.3.0
      - backtrader>=1.9.76
      - hydra-core>=1.3.0
      - mlflow>=2.8.0
      - jinja2>=3.1.0
      - pytest>=7.4.0
      - pytest-cov>=4.1.0
      - black>=23.0.0
      - ruff>=0.1.0
      - mypy>=1.5.0

# Optional GPU support (uncomment if needed)
# For RAPIDS cuML/cuDF, use:
# - cudatoolkit=11.8
# - pip:
#     - cudf-cu11
#     - cuml-cu11
#
# Or install via conda:
# conda install -c rapidsai -c conda-forge -c nvidia \
#     cuml=24.02 cudatoolkit=11.8 python=3.12

